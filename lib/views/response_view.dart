import 'package:flutter/material.dart';

class ResponseView extends StatelessWidget {
  const ResponseView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Response View'),
    );
  }
}
